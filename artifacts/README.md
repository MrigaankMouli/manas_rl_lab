# Artifacts

This directory is for generated outputs.

Expected future contents:

- checkpoints
- exported TorchScript or ONNX policies
- run logs
- evaluation JSON files
- rendered videos or rollout traces

Large generated files should usually be ignored by Git. Keep only this README or lightweight manifest files under version control unless a specific artifact is intentionally published.
