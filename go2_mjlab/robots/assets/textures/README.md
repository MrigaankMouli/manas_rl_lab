# Textures

This directory should contain texture files referenced by MJCF assets.

Expected future contents:

- robot visual textures
- material textures
- optional terrain textures

This directory is optional for training-only runs, but useful when rendering evaluation clips or debugging simulation visually.
