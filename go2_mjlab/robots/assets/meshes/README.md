# Meshes

This directory should contain mesh files referenced by the Go2 MJCF model.

Expected future contents:

- visual meshes for robot links
- collision meshes if used instead of primitive collision geometry
- any terrain or environment meshes that are robot-specific

Keep filenames stable because `go2.xml` will reference them directly.
